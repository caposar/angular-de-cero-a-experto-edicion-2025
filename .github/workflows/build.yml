name: Build
on:
  push:
    branches: [main]
    paths:
      - 'Angular/10-nest-teslo-shop-backend/**'
jobs:
  build:
    permissions:
      contents: read
      packages: write
    uses: caposar/mantenimiento-clientes/.github/workflows/build-reusable.yml@main
    with:
      imagen: teslo-shop
      contexto: Angular/10-nest-teslo-shop-backend