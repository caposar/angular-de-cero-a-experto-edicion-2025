
# Define el entorno de ejecución actual (ej. dev = 💻 Entorno Local, prod = 🌐 Entorno Online)
STAGE=dev

# Configuración de Base de Datos
DB_PASSWORD=MySecr3tPassWord@as2
DB_NAME=TesloDB
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres

# DB_SSL=false para Docker local | DB_SSL=true para BD online (Neon)
DB_SSL=false

# Configuración del Servidor
PORT=3000
HOST_API=http://localhost:3000/api

# JWT_SECRET: Clave criptográfica para firmar tokens. Mínimo 32 bytes en Base64/Hex (Puedes usar el Generador de Claves)
JWT_SECRET=Est3EsMISE3Dsecreto32s